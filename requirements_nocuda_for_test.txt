# CPU-only requirements for testing TASTE-SpokenLM streaming components
# Use this for testing on machines without GPU (e.g., Mac)

# Core ML libraries (CPU versions)
# Note: For uv, install torch separately: uv pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
torchaudio>=2.1.0

# Transformers and NLP
transformers>=4.35.0
tokenizers>=0.14.0
einops>=0.7.0
einx>=0.3.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.11.0
openai-whisper>=20231117

# Basic scientific computing
numpy>=1.24.0
pandas>=2.0.0

# Testing framework
pytest>=7.4.0
pytest-cov>=4.1.0

# Utilities
tqdm>=4.65.0
requests>=2.31.0
packaging>=23.0

# Optional: for ONNX models (CPU only)
onnxruntime>=1.16.0

# Development utilities
mock>=5.1.0
matplotlib>=3.5.0
hyperpyyaml>=1.2.0
omegaconf>=2.3.0
conformer>=0.3.0
diffusers>=0.21.0
lightning>=2.0.0